interface:
  display_name: "Openclaw Setup"
  short_description: "Repo-specific patterns and workflows for openclaw-setup"
  default_prompt: "Use the openclaw-setup repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true